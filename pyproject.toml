[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Himanshu", email = "himanshuchaudhary586@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=4.0.3",
    "pymongo>=4.18.0",
]

[project.scripts]
mcp-server = "mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
